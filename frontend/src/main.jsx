import ReactDOM from 'react-dom/client';
import react from 'react';
import App from './App.jsx';
import './src/src/index.css'; // Make sure this path is correct

ReactDOM.createRoot(document.getElementById('root')).render(
  <react.StrictMode>
    <App />
  </react.StrictMode>
);
